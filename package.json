{
  "name": "sptc-finance",
  "private": true,
  "version": "0.1.0",
  "description": "SPTC Finance - retail financing and EMI management platform",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "backend:dev": "npm run start:dev --workspace=apps/backend",
    "backend:build": "npm run build --workspace=apps/backend",
    "backend:test": "npm run test --workspace=apps/backend"
  },
  "devDependencies": {
    "wrangler": "^4.129.1"
  }
}
